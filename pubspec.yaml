name: caderneta_digital
description: "A new Flutter project for digital immunization records."
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.14.0
  sqlite3_flutter_libs: ^0.5.18
  path_provider: ^2.1.1
  path: ^1.8.3
  provider: ^6.1.1
  intl: ^0.19.0
  drift_flutter: ^0.2.0
  shared_preferences: ^2.2.2
  pdf: ^3.12.0
  printing: ^5.14.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  drift_dev: ^2.14.0
  build_runner: ^2.4.7

flutter:
  uses-material-design: true